import React, {Component} from 'react';

import APIUrl from '../../helpers/environment';
import img from '../../assets/start-a-band.gif'

const Header = () => {
    return (
        <header>
            <div>

            </div>
        </header>
    );
};

export default Header;